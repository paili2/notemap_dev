/** 🔍 키워드가 너무 광범위(당진, 포항 등)한지 판별 */
export function isTooBroadKeyword(raw: string): boolean {
  const keyword = raw.trim();

  if (!keyword) return true;
  // 한 글자/두 글자 너무 짧은 검색어 컷
  if (keyword.length <= 2) return true;

  // 동/읍/면/리/도로명/번지/건물·시설 키워드가 하나도 없으면 광역으로 간주
  const hasDetailToken =
    /(동|읍|면|리|로|길|\d|아파트|빌라|오피스텔|역|병원|의원|약국|마트|백화점|타워|센터|대학|대학교|고등학교|중학교|초등학교)/.test(
      keyword
    );

  return !hasDetailToken;
}
